import { Reveal } from "@/components/reveal";

export default function ShippingPage() {
  return (
    <main className="container-edit min-h-[60vh] max-w-2xl pb-28 pt-36">
      <Reveal>
        <h1 className="font-display text-4xl">Shipping &amp; Returns</h1>
        <p className="mt-6 text-sm leading-relaxed text-ink/60">
          This page is a placeholder for production shipping and returns
          copy. Replace with your reviewed policy before launch.
        </p>
      </Reveal>
    </main>
  );
}
